server:
  port: 8002
logging:
  level:
    root: INFO
    se.magnus.microservices: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product-db
